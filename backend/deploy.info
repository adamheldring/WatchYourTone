– run frontend build script
– that automatically copies frontend repo to backend public folder
– then commit all changes to backed git repo
– run heroku login
– push to heroku

git add .
git commit -m "new build thursday"
heroku login
git push heroku master

2022-10-27
Moved app to render.com
Due to Heroku no longer will support free hosting

